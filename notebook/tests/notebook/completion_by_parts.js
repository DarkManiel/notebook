//
// Tests completion by parts
//

casper.notebook_test(function () {
	this.test_completion = function (token) {
		this.then(function () {
            this.echo("Test token " + token);
        });

        // todo: finish test for completion by parts
	}
}